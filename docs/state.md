# Project state — AIFeeder (Mindful Content Feed)

## Current focus

**Assignment-ready: README + LICENSE + tests + source-delete + ingest panel + Phase 4 narrow all shipped this session.** The repo now meets the individual-use assignment brief: comprehensive [README.md](../README.md) (13 required sections, all present), MIT [LICENSE](../LICENSE), 43-test pytest suite in [tests/](../tests/) (all green in ~1.2s; covers URL detection, cascade delete, reason splitter, transcript chunker, exception classifier), [assets/](../assets/) placeholder for screenshots, dev-deps wired in [pyproject.toml](../pyproject.toml) `[project.optional-dependencies]`. Earlier same-session feature work below.

**Source-add ingest flow shipped + Phase 4 narrow + iframe + format icons + content-sized cards + source delete all shipped.** When user adds a source via the orange `+` button, after the source row is created the modal hands off to an "ingest panel" asking how many items to ingest (default 10, [1,100]); click Ingest → spinner during sync `process_source` (FastAPI thread-pooled) → "Done — N new items · cost $X.XXXX" panel; any close path reloads the page via `data-modal-needs-reload`. Source-edit modal now has a red `delete source` button on the bottom-left with `hx-confirm` browser dialog → `DELETE /sources/{id}` cascades through feedback → cost_log → summaries → items → sources in a single transaction. Batched-with-continue (the original ingest-flow ask) deferred to [ideas.md](ideas.md). A-decisions in [decisions.md](decisions.md); B-implementations in [engineering-decisions.md](engineering-decisions.md).

**Other same-session feature work:** YouTube ingestion via [`ingest.fetch_youtube_channel()`](../src/aifeeder/ingest.py) (yt-dlp + youtube-transcript-api, transcript chunked into pseudo-paragraphs). URL-detection in [`web/writes.py`](../src/aifeeder/web/writes.py) auto-sets `source_type='youtube_channel'`. Content page embeds `youtube-nocookie.com/embed/{video_id}` iframe above the transcript for YT items (no autoplay). Home cards show a coloured format icon (video / audio / reading) next to the apple verdict indicator, dispatched from `source_type`. **Home grid now uses `grid-auto-rows: auto` + dropped `.shape-text` row span** — research-paper / essay cards no longer 2x too tall; rows size to their tallest card. Next: real-world test + decide on remainder of phase-4-full (chapters + real notes/favs schema migration). Thumbnails on home cards remain decided against per [decisions.md → 2026-05-21 no thumbnails in v1](decisions.md) (mindfulness paradox).

Working title: *Mindful Content Feed* — intent-filtered, mindful consumption. v1 audience: the builder.

## Recent decisions

Full A-category reasoning in [`docs/decisions.md`](decisions.md); B-category in [`docs/engineering-decisions.md`](engineering-decisions.md). UX/UI deliberation in [`docs/conversation/2026-05-20_b670_ux-design.md`](conversation/2026-05-20_b670_ux-design.md). Most relevant since last refresh:

- **README + LICENSE + tests/ shipped (assignment-ready packaging).** New [README.md](../README.md) (13 required sections: Overview→Problem+Outcome, Demo with 7 screenshot placeholders, Frontend+Backend stacks, Development Approach with AI including 4 roles + key prompts + 8 review points, Installation, Usage, Project Structure, Reflection); MIT [LICENSE](../LICENSE); new [tests/](../tests/) directory with `conftest.py` (tmp_db fixture monkeypatching `AIFEEDER_DB` per test) + 43 tests across `test_writes.py` (URL detection, cascade delete, insert-with-auto-detect), `test_routes_pure.py` (palette/format dispatch + the brittle reason splitter), `test_ingest.py` (transcript chunker with mock snippets), `test_refresh.py` (exception → failure_class). All green in ~1.2s. `pytest>=8.0` added to [pyproject.toml](../pyproject.toml) `[project.optional-dependencies] dev`; `[tool.pytest.ini_options]` sets `testpaths = ["tests"]` + `pythonpath = ["src"]`. `assets/` directory created with a placeholder README listing the 7 suggested captures.
- **Source delete shipped (red button on edit modal).** Red button with trash icon, bottom-left of `partials/source_modal.html` (edit mode only). HTMX `hx-confirm` for the destructive guard (browser-native `confirm()` with the source name + "this cannot be undone"). `DELETE /sources/{id}` route → `delete_source()` in [`web/writes.py`](../src/aifeeder/web/writes.py) does hard-delete with explicit 5-step cascade in a single transaction (feedback → cost_log → summaries → items → sources, since schema has no `ON DELETE CASCADE`). 204 + `HX-Refresh: true` on success. Smoke-tested both happy path + cascade (5 tables all drop to 0) + 404-on-missing. [engineering-decisions.md → 2026-05-22 Source delete](engineering-decisions.md).
- **Source-add ingest flow shipped (A + B decisions same turn).** When user adds a source, modal hands off to ingest panel: count input (default 10, [1,100]) → spinner → done panel (cost + summary) → reload. Reuses `process_source` from [refresh.py](../src/aifeeder/refresh.py). Close behavior: new `data-modal-needs-reload` attribute on the modal backdrop; close JS in [main.js](../src/aifeeder/static/js/main.js) checks for it and triggers `window.location.reload()` on any dismiss (X / Esc / backdrop / Cancel). User had originally proposed batched-with-continue with running cost display + Done-first button order + soft cost-threshold guard; redirected to simpler single-shot upfront count and asked the batched-continue idea be parked. A-decision in [decisions.md → 2026-05-22 Source-add ingest flow](decisions.md); B-implementation in [engineering-decisions.md → 2026-05-22 Source-add ingest panel implementation](engineering-decisions.md); deferred alternative in [ideas.md → Batched-continue ingest flow with running cost display](ideas.md).
- **Home cards now size to content (browser-check fix).** User screenshotted the post-format-icons home grid showing research-paper / essay cards ~2x taller than their text needed (root cause: `grid-auto-rows: 220px` + `.shape-text { grid-row: span 2 }` forced those cards to 440px). Switched [cards.css](../src/aifeeder/static/css/cards.css) to `grid-auto-rows: auto` + dropped the row-span + added `min-height: 180px` floor on `.card`. Rows now size to the tallest card in the row. Kept `.shape-video` column-span and the title-size bump on `.shape-text` (visual emphasis decoupled from card height). Cards within the same row still match heights (CSS Grid limitation; true per-card masonry would need JS or CSS columns). [engineering-decisions.md → 2026-05-22 Home cards size to content](engineering-decisions.md).
- **Content-page iframe + home-card format icons shipped (same session).** User asked for thumbnails + iframe; I flagged thumbnails as A-reconsider (mindfulness paradox lock from 2026-05-21 still standing) and user confirmed *no thumbnails*, *yes iframe*, plus a new ask for **coloured format-type icons** (video / audio / reading) next to the apple. Built six-choice cluster: format dispatched from `source_type` (not AI-derived `content_type_tag` which drifts); video_id derived from `external_id` (no schema change); `youtube-nocookie.com` embeds (no autoplay); iframe above transcript (both visible); icon left of apple at 22px / 0.85 opacity using teal / sienna / muted-brown. Logged in [engineering-decisions.md → 2026-05-22 Content-page iframe + home-card format icons](engineering-decisions.md).
- **Phase 4 narrow shipped — YouTube ingestion via yt-dlp + youtube-transcript-api.** Added `yt-dlp` + `youtube-transcript-api` to deps. New `fetch_youtube_channel(url, limit)` in [ingest.py](../src/aifeeder/ingest.py) enumerates newest videos (drills through channel-root → /videos sub-playlist by channel_id), pulls transcripts, chunks into pseudo-paragraphs (`_transcript_to_paragraphs`: > 2.5s gap or 600-char sentence boundary). New `fetch_for_source(source, limit)` dispatcher. [refresh.py:209](../src/aifeeder/refresh.py#L209) now calls dispatcher. [web/writes.py.insert_source()](../src/aifeeder/web/writes.py) auto-detects source_type from URL host (youtube.com / youtu.be / m.youtube.com / www.youtube.com → `'youtube_channel'`; else `'rss'`). Smoke-test green on Struthless: 3 videos at $0.0036, all locked tone phrasings ("Worth reading if…", "Worth a shot for X although Y", "Skip — this is mostly Z") preserved. Six B-choices logged in [engineering-decisions.md → 2026-05-22 Phase 4 narrow shipped](engineering-decisions.md).
- **Source-add UI shipped — phase 3 of the locked post-refresh order.** Orange `+` button in sidebar now opens the source-edit modal in add-mode (one shared template, branches on `is_add = not source.id`). New `insert_source(name, url, why)` in [`web/writes.py`](../src/aifeeder/web/writes.py) (hardcoded `source_type='rss'`; YouTube toggle deferred to phase 4). New routes `GET /sources/new` + `POST /sources` in [`web/routes.py`](../src/aifeeder/web/routes.py) registered BEFORE `/sources/{source_id}` per the Starlette route-order gotcha. Server-side validates all-three-required → modal-error; `sqlite3.IntegrityError` on duplicate URL → modal-error; success → 204 + `HX-Refresh: true`. Six B-choices logged in [engineering-decisions.md → 2026-05-22](engineering-decisions.md).
- **`aifeeder refresh` shipped per the locked 10-step plan.** New [`src/aifeeder/refresh.py`](../src/aifeeder/refresh.py) (~245 LOC): `classify_exception` lookup, `summarize_with_retry` (3x transient 1s/4s/16s, 1x unknown then permanent), per-item-commit `persist_summary`, never-raises `process_source`, `run_refresh` top-level. [`cli.py`](../src/aifeeder/cli.py) wires `_cmd_refresh` with `--per-source` (default 10) / `--source-id` / `--dry-run` / `--quiet`. Verbose-default (per locked decision). Smoke-test result: 4 items processed at $0.0015 total; dedup verified both ways (insert via `--dry-run`, summarize via re-run). UI integration confirmed: `/home` renders real items with locked AI tone phrasings naturally appearing. Plan-cluster + implementation cluster both in [engineering-decisions.md → 2026-05-22](engineering-decisions.md).
- **Home card splits "maybe" reason into stacked green/orange blocks (labelless).** Sage left-border + faint sage bg = "for you" (positive); warm-sienna left-border + faint sienna bg = caution. "Yes" verdicts render just the green block. Stems ("Worth a shot for…" / "although…" / "Worth reading if…") kept verbatim — locked AI voice preserved 1:1. Split happens client-side in `_split_reason()` regex on `(although|but|though)`. New sage palette token `--accent-positive*` added since warm/cool/neutral had no green. A-decision: [decisions.md → 2026-05-22](decisions.md). Tradeoff flagged: brittle splitter; mitigation deferred to a `reason_caution` field in the AI tool schema.
- **Sources are user-editable from the UI** — first UI-→-DB write path in v1. Click a sidebar source → centered modal (backdrop blurs main column, sidebar z-indexed above), URL + why fields, save persists via new [`web/writes.py`](../src/aifeeder/web/writes.py) using `UPDATE sources SET url = ?, why = ?`. Knock-on: the modal pattern becomes the in-house convention; `repo.py` stays read-only by design; `web/writes.py` is the natural home for future archive/delete/mark-read UI writes.
- **First browser-check feedback round (5 A-decisions + 9 B-decisions)** — thumbs supersedes the locked "icon arrows not thumbs"; plain-block content callout supersedes the sticky-note; Source Sans 3 body typography refines the 2026-05-20 typography lock; apple yes/maybe indicators on home cards (whole = yes, 3/4-eaten = maybe); in-text highlighting + quote-attached notes (B→A escalation — touches scope, schema-shape, and the note-taking interaction model).
- **Avatar restructure** — the shaded radial-gradient circle is now the *head* (not a background); a matching shaded dome below it is the *body*; dark-brown silhouette layer removed entirely. Supersedes the round-1 "silhouette-inside-the-circle" engineering fix.
- **Card title highlight (3 iterations)** — final shape: per-line bands with transparent top/bottom margins via `linear-gradient(transparent 12%, accent 12%, accent 88%, transparent 88%)` + `box-decoration-break: clone`.

### v1 slice scope (extended)

- **Sources:** Simon Willison (RSS) + ArXiv `cs.AI` (RSS) + **Struthless (YouTube, shipped)**. Other YT channels can be added via the orange `+` button (URL-detected).
- **In:** RSS ingest, **YouTube ingest (yt-dlp + youtube-transcript-api, narrow scope)**, AI summarize, list view, detail view, manual refresh (CLI, shipped), "open original" link, faked notes + favourites UI, source edit + add (URL + why) — real DB writes.
- **Out (schema ready, UI deferred or stubbed):** real thumbs feedback persistence, library archive UI, sortable list, cost dashboard, chapter index, periodic "why" revisit, **real notes/favs/highlights schema (phase-4-full)**.

### Failure recovery (now wired)

Transient (urllib URLError, socket.timeout, openai RateLimitError / APIConnectionError / APITimeoutError) → 3 retries with exponential backoff (1s/4s/16s); permanent (openai AuthenticationError) → no retry, `status='failed'`; content (json.JSONDecodeError) → no retry, `status='skipped'`; unknown → 1 retry then recorded as 'permanent'. Source-fetch failure increments `consecutive_failures` + sets `last_error_at/message`; success resets to 0. Sources with `consecutive_failures >= 3` get a `!` icon (UI hook still pending — flag rendering not yet wired into the sidebar template).

## What's implemented

- `aifeeder init` — apply schema, seed sources + user row
- `aifeeder preview` — fetch + summarize + stdout, no DB writes (validated end-to-end)
- `aifeeder refresh` — fetch + dedup-insert + summarize-only-`pending` + per-item-commit. Now dispatches on `source_type`: RSS via feedparser, YouTube via yt-dlp + youtube-transcript-api. Flags: `--per-source N` (default 10), `--source-id N`, `--dry-run`, `--quiet`. Verbose-default.
- `aifeeder seed-fake` — 12 fixture items spanning all 9 content_type_tags for UI dev (idempotent)
- `aifeeder serve` — local FastAPI + HTMX + Jinja2 UI on 127.0.0.1:8000
- **Web UI** —
    - Loading → home → content + Library/Favourites/Notes placeholder pages
    - **Sidebar:** Home button + Library/Favourites/Notes + alphabetical source list (HTMX-triggers source-edit modal); **orange `+` button now opens add-source modal**; shaded-head + shaded-body avatar with thought-bubbles on hover; expand-handle tab visible when collapsed
    - **Home cards:** apple verdict indicators (whole=yes, 3/4-eaten=maybe), Source Sans 3 body font, per-line title highlights with transparent top/bottom margins, **stacked sage/sienna reason+caution blocks (labelless, color-only)**
    - **Content page:** plain-block "why" callout (purpose + key_points), thumbs up/down feedback, in-text selection → floating "+ Note" → quote-attached note + persistent `<mark>` highlight on reload; **`youtube-nocookie.com/embed/{video_id}` iframe above the transcript for YouTube items** (no autoplay; 16:9 responsive container)
- **Home cards:** also show a coloured **format icon** (video / audio / reading) next to the apple verdict indicator, dispatched from `source_type` (video=teal, audio=sienna, reading=muted-brown)
    - **Source add + edit modal** — single shared template (`partials/source_modal.html`) branches on `is_add = not source.id`. Add mode: Name + URL + Why fields, all HTML5-`required`; save = `POST /sources` → hands off to **ingest panel** (`partials/ingest_panel.html`) in the same modal slot, asking how many items to ingest (default 10, [1,100]); click Ingest → spinner during `POST /sources/{id}/ingest` (sync `def`, FastAPI thread-pooled; reuses `process_source` from refresh.py) → done panel showing cost + counts → reload via `data-modal-needs-reload`. Edit mode: URL + Why only (Name lives in the H2 title); save = `POST /sources/{id}` → `HX-Refresh: true` directly. Both: centered modal blurs main column (sidebar stays sharp via `z-index: 250` above backdrop `z-index: 200`); close on Esc / backdrop-click / Cancel; duplicate-URL or empty-fields gracefully re-render the modal with a `.modal-error` callout (not 500)
- **HTMX endpoints:** feedback callout, note panel (with quote pre-fill), favourite toggle, source modal (add + edit + save), source ingest panel + ingest run + done panel, **source delete (DELETE /sources/{id} with hx-confirm)**
- 7 page routes + 11 HTMX endpoints return 200 in smoke test; happy-path verified for source add → ingest panel → done panel + source delete (204 + HX-Refresh, cascade drops 5 tables to 0, 404 on missing)

## Open questions

1. **Source of "user-level whys" (deferred).** v1 loading page uses per-source whys directly. AI-derived user-level intent statements remain a future surface, not blocking.
2. **Ritual fit.** Replace doomscrolling without becoming another "must check" app. Load-bearing through build.
3. **Home page intentionality.** "Anything AI recommended" is the v1 default; the question of *what should the home page intentionally show* (unread-only? today's-only? hand-curated?) is provisional — see [decisions.md → 2026-05-21 — Home cards stay unchanged](decisions.md). Affects the read-state dimming idea ([ideas.md → UI/UX](ideas.md)).
4. **iframe vs. recommendation-surface leak.** Not relevant in v1 (no YouTube). Tracked for v2.
5. ~~**Add-source affordance.**~~ **Done.** Orange `+` button now opens the source-add modal (one shared template with edit mode). Source-delete UI remains deferred.

## Next steps

In order:

1. **Real-world test** — `aifeeder refresh` (lift the `--per-source` cap or leave at 10), use `serve` daily for a few days. Add a third source via the new `+` button to exercise the add flow on something real. Watch for: AI verdict quality on the live feed; refresh latency / cost surprises; UX papercuts on the sage/sienna split (is the contrast loud enough on warm-cream cards? does the brittle splitter misfire on any real "maybe"?); source-health icon rendering (need to wire the `!` icon for `consecutive_failures >= 3` into the sidebar template — currently the column is populated but no UI surfaces it); does the add-source modal feel right with a real new source (e.g. Latent Space, Hacker News specific tag)? Fix top 1–2 issues.
2. **Phase-4-full remainder — open call after real-world test.** Iframe shipped this session, so what's left of phase-4-full is: chapter handling (transcript timestamps vs yt-dlp's chapter metadata — still open A-Q; yt-dlp gives them for free when present) + migrate notes/favourites/highlights from `web/fakes.py` in-memory dicts to real schema tables (three new tables, new write paths in `web/writes.py`, template plumbing). The notes/favs/highlights migration is non-trivial and could ship as its own session.
3. **Transcript readability papercuts surfaced during smoke test** (defer; surface when annoying):
   - Auto-caption `>>` speaker markers leak through the chunker (visible in the rendered paragraphs). Fix: regex-strip leading `>>` per snippet in `_transcript_to_paragraphs`.
   - Snippets are sometimes split mid-word by YouTube auto-captions ("role playinging" in the smoke test). Not fixable from our side.
   - 36k-char transcripts ≈ 9k input tokens — ~12x the cost of an RSS item ($0.0015 vs $0.0001). Add a `--max-transcript-chars` truncation flag if a channel produces consistently long videos.

## Gotchas / quirks

- **UI iteration round 1 + 2 done; round 3 = post-refresh sage/sienna reason-split.** Browser-checked refresh in `/home`, liked it, asked for the visual split — landed labelless (color + position only). Locked AI phrasings preserved verbatim.
- **Reason-splitter is brittle by design.** Client-side regex on `\s+(although|but|though)\s+` (case-insensitive) inside [`routes.py:_split_reason()`](../src/aifeeder/web/routes.py). 24/24 sampled outputs split cleanly, but a model swap or prompt tweak that introduces "however" / "yet" / "though arguably" would silently produce a single green block with no caution. Future fix: add an explicit `reason_caution` field to the OpenAI tool-call schema in [ai.py](../src/aifeeder/ai.py) — deferred, not blocking.
- **`aifeeder.db.seedfake.bak` exists** at the project root — backup of the seed-fake DB before `refresh` shipped, kept in case visual seed-fake inspection is ever useful. Untracked. `rm` it whenever.
- **Modal pattern is the in-house convention.** `partials/source_modal.html` + `.modal-backdrop` + `.modal-card` + Esc/click-outside/cancel close. Future modals reuse this shape rather than inventing a new one. **Add + edit share this template** — branches on `is_add = not source.id`. **Multi-step flow pattern:** when a modal step needs the page to reload on dismiss (e.g. ingest_panel after source was created), set `data-modal-needs-reload` on the backdrop; [main.js `closeModal()`](../src/aifeeder/static/js/main.js) detects this attribute and triggers `window.location.reload()` after clearing the slot, instead of only clearing. Used today by `ingest_panel.html` + `ingest_done.html`.
- **`process_source` is now imported from refresh.py into the web layer.** [`web/routes.py`](../src/aifeeder/web/routes.py) `POST /sources/{id}/ingest` calls `process_source(conn, source, per_source=N, verbose=False)` directly — no behaviour drift between CLI `aifeeder refresh` and web ingest. The web ingest route is a sync `def` (not async) so FastAPI runs it in a thread pool; this avoids blocking the event loop on 30–60s YouTube fetches.
- **RSS ingest "count=N" inserts the whole feed but only summarizes N.** Existing `process_source` semantics: feedparser returns all available entries → `insert_new_items` dedup-inserts all of them → summarize loop is capped at `per_source`. So "ingest 3 items" on a 20-entry feed creates 17 latent `pending` rows that get summarized on the next `aifeeder refresh`. Not a regression — but means the user-visible "Done — 3 new items" count is feed-eligible items, not items inserted into DB. If this becomes confusing, cap `insert_new_items` to `per_source` as a follow-on.
- **`web/writes.py` is the first real UI-→-DB write path.** `repo.py` stays read-only by design; `fakes.py` stays for in-memory fakes (notes/favourites/highlights). New UI writes that need real persistence go in `writes.py`.
- **`sources.url` carries a `UNIQUE` constraint.** Editing a source's URL to one already used by another source raises `sqlite3.IntegrityError`; the route catches this and re-renders the modal with a `.modal-error` callout (no 500). Worth remembering when wiring `refresh` (which also inserts sources via `INSERT OR IGNORE`).
- **Notes + favourites + highlights are still UI-faked** — in-memory dicts in [`src/aifeeder/web/fakes.py`](../src/aifeeder/web/fakes.py); wiped on server reload. Real schema migration was originally coupled to the YouTube/Struthless session; narrow phase 4 shipped without it, so the migration now lives in phase-4-full as its own slice of work. Highlights use naïve substring-match (first occurrence per paragraph) — corner case if the same phrase appears twice.
- **YouTube ingestion details to remember.** Channel-root URLs (e.g. `youtube.com/@Handle`) return a playlist of sub-playlists (Videos / Shorts / Live); `fetch_youtube_channel` detects this and re-extracts using `/channel/{channel_id}/videos`. Transcripts come from `youtube-transcript-api` (not yt-dlp's subtitle URLs — those need fragile JSON3/VTT dedup). `_transcript_to_paragraphs` breaks on > 2.5s gap OR sentence-boundary after 600 chars — heuristic, leaks `>>` speaker markers and occasionally splits sentences awkwardly. yt-dlp first-fetch on a 100-video channel takes 30–60s; budget for the latency in real-world use.
- **Two deps added (yt-dlp, youtube-transcript-api).** `youtube-transcript-api` brought transitive deps too (requests, urllib3, charset-normalizer, defusedxml). `.venv` was installed with `uv pip install -e .` (no pip in `.venv/bin/` — use uv).
- **YT transcript cost is ~12x an RSS item** (~$0.0015 per video vs ~$0.0001 per RSS item on Struthless, GPT-4o-mini). Bounded by `--per-source` cap. No content truncation today; add `--max-transcript-chars` if a channel produces consistently long videos.
- **Quote-attached note save uses `HX-Refresh: true`** so the page reloads with the new `<mark>` rendered server-side. Same pattern reused by the source-edit save.
- **Two Starlette gotchas baked into routes.py** — `TemplateResponse(request, name, ctx)` signature (request first); FastAPI route order matters (`/feedback/{id}/refine` BEFORE `/feedback/{id}/{direction}`; `GET /sources/new` + `POST /sources` BEFORE the `/sources/{source_id}` variants — literal paths must register first so they win over `{template}` matches).
- **`users` table addition partially supersedes the 2026-05-20 local-only "no forward-compat scaffolding" line** — the `users` parent table is in v1; `items.user_id` etc. are still deferred. See [`decisions.md`](decisions.md) → 2026-05-21 — `users` table.
- **`docs/handoff.md` and `docs/my-log.md` intentionally untracked** — never `git add .` or `-A`; always stage explicitly. The `/ship-checkpoint` skill stages by path.
- **Three parallel Claude Code sessions ran on this checkout earlier** (b670 = strategy + scaffold + AI + UX + UI + iteration; 10ca = hook debugging; 4dac = git/auth setup). `state.md` is single-writer — serialise `/state` between concurrent sessions or use `git worktree`.
- **Schema includes future-MVP columns the slice doesn't use yet** (`failure_class`, `failure_message`, `consecutive_failures`, `last_error_at`, `last_error_message`, `is_read`, `feedback` table, `chapter_index_json`).
- **`init` + `seed-fake` are idempotent** — `init` applies schema + seeds sources + user; `seed-fake` inserts 12 items only if missing. Re-runs are no-ops. **Caveat:** if a source's URL has been UI-edited between init runs, the original seeded URL becomes "available" again and a re-`init` will create a duplicate source row (discovered + cleaned up during smoke test). Same caveat now applies to UI-added sources via the orange `+` button — re-`init` will not touch them but a subsequent edit to one of the seeded URLs followed by re-`init` would re-create the seeded row alongside the edited copy.
- **DB file is local-only.** If `aifeeder.db` exists from before the `users`-table addition, `serve` will work but `get_user()` returns the fallback `{"id": 1, "name": "you"}` instead of `Emma` — delete the file and re-`init` to fix.
- **"Open original" link bypasses in-app reader in v1 slice.** Fine for Simon + ArXiv. Mission-compromise flag lights up when YouTube enters v2.
- **Never instrument engagement metrics** (DAU, session length). Mission-aligned metrics only.
- **The mindfulness paradox** — every UX decision: if it would feel at home in TikTok, it doesn't belong here.
- **AI prompt tone is A-category.** Locked phrasings ("Worth reading if…" / "Worth a shot for X although Y" / "Skip — this is mostly Z") — never change without user sign-off.
- **Conversation log format:** per-turn entries are brief **Me:** / **Claude:** intent-summary — never a verbatim repaste of the chat reply, even when in-character. Memory rule: [feedback_conversation-log-no-transcripting](/home/emma/.claude/projects/-home-emma-Workspace-Make-Land-AIFeeder/memory/feedback_conversation-log-no-transcripting.md).
- **Completeness rule for decisions** — every concrete decision reached must land in decisions.md (A) or engineering-decisions.md (B) the same turn, not only the conversation log. See [CLAUDE.md → Completeness rule](../CLAUDE.md#completeness-rule--no-concrete-decision-lives-only-in-conversation). Heavily exercised today (6 A-entries + 16 B-entries logged across 5 user turns).
- **IDE flags `schema.sql` errors** — false positives from the MSSQL language server; file is valid SQLite.
- **Tests run with `pytest` after `pip install -e ".[dev]"`** (or `uv pip install pytest --python .venv/bin/python`). `conftest.py`'s `tmp_db` fixture is the canonical way to test anything DB-touching — monkeypatches `aifeeder.db.DB_PATH` to a per-test tmp file, applies schema fresh.
- **A parallel session (945d) is running on this checkout** — its conversation file at `docs/conversation/2026-05-22_945d_interview-prep.md` is untracked. State.md is single-writer; serialize `/state` between the two sessions or use git worktree.
- **`assets/` is empty except for a placeholder README** — user takes the 7 screenshots referenced from README.md (`home-grid.png`, `add-source-modal.png`, `ingest-panel.png`, `ingest-done.png`, `content-page-rss.png`, `content-page-youtube.png`, `delete-confirm.png`) after this commit.

_Last updated: 2026-05-22 — Assignment-ready packaging shipped (README + MIT LICENSE + 43-test pytest suite + assets/ placeholder). Source delete + ingest flow + Phase 4 narrow + iframe + format icons + content-sized cards all also shipped this session. Next: user takes the 7 screenshots + commits + pushes; then real-world test + decide on phase-4-full remainder._
