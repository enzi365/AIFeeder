# assets/

Screenshots, GIFs, or short demo clips referenced from the top-level [`README.md`](../README.md).

Suggested captures (referenced by name from README.md):

- `home-grid.png` — the `/home` feed showing mixed RSS + YouTube items, sage/sienna reason-split callouts, format icons, apple verdict indicators.
- `add-source-modal.png` — the orange `+` button modal (Name / URL / Why).
- `ingest-panel.png` — the post-add ingest panel asking how many items to fetch.
- `ingest-done.png` — the result panel ("Done — N new items · cost $0.XXXX").
- `content-page-rss.png` — a content page for an RSS item showing the purpose+key-points callout above the reader paragraphs.
- `content-page-youtube.png` — a content page for a YouTube item showing the callout, the embedded iframe, and the chunked transcript below.
- `delete-confirm.png` — the source-edit modal with the red delete button + browser `confirm()` dialog.
