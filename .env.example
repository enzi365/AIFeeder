# LLM provider configuration
OPENAI_API_KEY=
# ANTHROPIC_API_KEY=  # optional, uncomment to use Claude

# Default model for summarization
AIFEEDER_MODEL=gpt-4o-mini

# Optional: override default DB path (defaults to ./aifeeder.db)
# AIFEEDER_DB=./aifeeder.db
